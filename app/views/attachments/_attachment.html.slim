div[id="attachment_#{a.id}"]
  = link_to a.file.identifier, a.file.url
  - if current_user && current_user.author_of?(a.attachmentable)
    =link_to 'Delete attach', attachment_path(a), method: :delete, remote: true